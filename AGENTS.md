# 프로젝트 작업 규칙

이 저장소에서 작업하는 모든 개발 에이전트는 `PRD.md`를 제품 요구사항의 최우선 기준으로 사용한다. 전체 시스템의 부품, 책임, 데이터 흐름과 보안 경계는 `docs/SYSTEM_ARCHITECTURE.md`를 따르고, 구현 순서와 작업 단위는 `docs/DEVELOPMENT_PRIORITY.md`를 따른다.

문서의 역할은 `PRD.md`가 **무엇을**, `docs/SYSTEM_ARCHITECTURE.md`가 **어떤 구조로**, `docs/DEVELOPMENT_PRIORITY.md`가 **어떤 순서로** 만들지를 정하는 것이다. 제품 범위가 충돌하면 `PRD.md`가 우선하고, 구현 순서는 `docs/DEVELOPMENT_PRIORITY.md`가 우선한다. 충돌 사실과 판단 근거는 해당 작업의 개발일지에 기록한다.

## 0. 최종 산출물과 필수 플러그인

최종 프로젝트는 아래 세 항목으로 제출하고, 각 항목은 지정된 플러그인으로 실제 상태를 확인해야 한다. 플러그인은 개발을 돕는 연결 도구이며, 호출했다는 사실만으로 배포·저장·자동 수집이 완료된 것은 아니다.

| 최종 산출물 | 반드시 사용할 도구 | 완료 증거 |
| --- | --- | --- |
| 최종 배포 URL | Vercel 플러그인 | 프로덕션 배포 성공 상태, 실제 HTTPS URL, 테스트한 배포 버전 |
| 데이터베이스: 회원·서비스 데이터·사용로그 | Supabase 플러그인 | 대상 프로젝트, 실제 테이블·마이그레이션, RLS·권한, Security Advisor 결과 |
| 공지사항 크롤링 | Apify MCP | 세 출처 Actor/run ID, 추출 필드, 수동 실행 절차, 최근 실행 결과, Supabase 반영 표본 |

쉬운 뜻:

- **Vercel 플러그인**은 완성된 앱을 인터넷에 올리고 실제 주소가 열리는지 확인할 때 쓴다.
- **Supabase 플러그인**은 데이터베이스가 설계대로 만들어졌고 공개 사용자가 데이터를 함부로 바꾸지 못하는지 확인할 때 쓴다.
- **Apify MCP**는 학교 홈페이지 세 곳에서 공지 제목·날짜·원문 링크를 가져오는 Actor 실행과 결과를 확인할 때 쓴다.

현재 PRD는 로그인과 사용자별 서버 저장을 제외한다. 따라서 최종 데이터베이스 항목의 **회원**은 `사용하지 않음`으로 보고하고 회원 테이블을 억지로 만들지 않는다. **서비스 데이터**는 `notices`, **운영로그**는 `crawl_runs`이며, 개인별 행동을 추적하는 사용로그는 수집하지 않는다. 자세한 구분은 `docs/ERD.md`를 따른다.

Apify MCP 사용은 현재 사용자에게서 추가된 필수 구현 제약이다. 기존 문서나 작업 단위에 자체 `fetch`·`cheerio` 크롤러가 적혀 있어도 새 구현을 시작하지 않는다. Apify Actor가 HTML 추출을 담당하고, 앱 코드는 결과 검증·정규화·Supabase 반영만 담당한다. W10에 들어가기 전에 관련 PRD·시스템 구조·우선순위 문서를 이 결정과 일치시키고 개발일지에 변경 이유를 남긴다.

공지 갱신은 해커톤 MVP에서 자동 예약하지 않는다. 운영자가 Codex에 `공지 갱신해줘`라고 요청할 때만 Apify Actor 실행 세 개를 시작하고 Supabase에 반영한다. 일반 사용자가 보는 앱에는 크롤링 시작 버튼이나 비밀 키를 넣지 않는다. 자동 갱신과 Vercel Cron은 사용자가 다시 요청하기 전까지 구현하지 않는다.

실제 수동 실행 절차와 성공 보고 형식은 `docs/MANUAL_NOTICE_REFRESH.md`를 따른다.

필수 플러그인을 현재 세션에서 찾을 수 없거나 연결하지 못하면:

1. 설치·연결 상태와 계정·요금제 의존성을 확인한다.
2. 플러그인 없이 가능한 독립 작업만 수행한다.
3. 지정되지 않은 도구로 최종 증거를 대신하지 않는다.
4. 필요한 사용자 행동과 바로 이어서 할 첫 행동을 `blocked` 개발일지에 남긴다.
5. 특히 Apify MCP가 없다는 이유로 자체 크롤러를 대신 구현하지 않는다.

비밀값인 Supabase secret key, Vercel token, Apify API token은 출력하거나 Git에 저장하지 않는다. Apify OAuth 연결 또는 배포 서비스의 비밀 저장소만 사용한다.

## 1. 세션 시작 절차

새 개발 세션에서는 코드를 수정하기 전에 반드시 다음 순서로 현재 상태를 파악한다.

1. `PRD.md`를 읽는다.
2. `docs/SYSTEM_ARCHITECTURE.md`를 읽고 현재 작업의 담당 부품, 데이터 흐름, 브라우저/서버 및 공개/비밀 경계를 확인한다.
3. `docs/DEVELOPMENT_PRIORITY.md`를 읽고 가장 앞선 미완료 작업 단위를 확인한다.
4. `docs/dev-logs/`의 개발일지를 파일명 역순으로 확인한다.
5. 가장 최근의 `완료` 일지와, 존재한다면 가장 최근의 `진행 중` 또는 `막힘` 일지를 읽는다.
6. 실제 코드와 테스트 상태를 확인하여 일지의 인수인계 내용이 현재 저장소와 일치하는지 검증한다.
7. 선행 작업이 완료된 작업 단위 중 우선순위가 가장 높은 **한 개**를 이번 세션의 작업 단위로 선택한다.
8. 선택한 작업에 필요한 Vercel·Supabase·Apify MCP가 현재 세션에서 실제로 사용 가능한지 확인한다. 이전 세션의 설치 상태를 그대로 가정하지 않는다.

최신 일지만 읽어서는 맥락이 불충분할 때에는 연결된 이전 작업 ID의 일지도 함께 읽는다. 일지 내용만 믿고 구현 완료를 가정하지 않는다.

## 2. 작업 단위 원칙

- 한 세션에서는 원칙적으로 하나의 작업 단위만 구현한다.
- 작업 단위의 범위는 `docs/DEVELOPMENT_PRIORITY.md`에 정의된 완료 조건과 필수 검증까지 포함한다.
- 현재 작업에 필요한 최소한의 선행 수정은 허용하지만, 다음 작업 단위의 기능을 미리 확장하지 않는다.
- PRD의 명시적 제외 기능은 구현하지 않는다.
- 디자인 장식보다 `지도 → 장소 소개 → 사진 제출 → 캐릭터 획득 → 도감 저장` 흐름의 안정성을 우선한다.
- 캐릭터 이미지를 생성하거나 교체하기 전에는 `PRD.md` 8장과 `CHA-CHA)Design System/readme.md`를 읽고, `CHA-CHA)Design System/assets/brand-sheet.png` 및 5방향 `mascot-*.png`를 시각적 기준으로 사용한다. 장소별 의상·소품만 다르게 만들고 기본 얼굴·체형·색상·비율은 유지한다. 제공된 원본 이미지 자체를 재색칠·회전·좌우 반전·비율 왜곡하지 않는다.
- `CHA-CHA)Design System/`을 읽을 수 없으면 캐릭터를 추측해 최종 생성하지 않는다. 규격이 같은 임시 자산으로 진행하고, 막힌 이유와 필요한 자료를 개발일지에 기록한다.
- `.env`, `.env.local`의 비밀값을 출력하거나 커밋하지 않는다. 예시 파일에는 변수명만 둔다.
- 시스템 부품의 책임, 데이터 저장 위치, 브라우저/서버 경계 또는 보안 경계를 바꾸는 구현은 `docs/SYSTEM_ARCHITECTURE.md`도 같은 세션에 갱신한다.
- `docs/SYSTEM_ARCHITECTURE.md`의 목표 구조를 이미 구현된 상태로 가정하지 않고 실제 코드와 테스트로 확인한다.
- Supabase 관련 작업은 Supabase 플러그인으로 실제 프로젝트 상태를 먼저 읽고, 변경 후 테이블·권한·보안 Advisor를 다시 검증한다.
- 공지 HTML 추출 작업은 Apify MCP Actor에만 둔다. 애플리케이션 코드는 Apify 결과를 검증하고 공통 형식으로 바꿔 Supabase에 저장할 수 있지만 학교 홈페이지를 직접 파싱하지 않는다.
- Vercel 배포 작업은 Vercel 플러그인으로 실행·확인하고, 최종 URL에서 핵심 흐름을 브라우저로 별도 검증한다.

## 3. 완료 판정 게이트

작업 단위는 다음 조건을 모두 만족해야 `완료`로 판정한다.

1. 해당 작업 단위의 구현이 끝났다.
2. 정의된 완료 조건을 모두 확인했다.
3. 관련 자동 테스트, 타입 검사, 린트, 빌드 중 프로젝트에서 실행 가능한 검증을 모두 통과했다.
4. 사용자 흐름이 포함된 작업은 수동 검증 또는 브라우저 검증을 수행했다.
5. 실패·오프라인·빈 상태처럼 PRD가 요구하는 예외 경로도 확인했다.
6. 코드, 테스트 결과, 남은 위험이 서로 일치한다.
7. 해당 작업에 지정된 플러그인으로 외부 시스템의 실제 결과를 확인했다.

검증을 실행할 수 없는 환경이면 성공으로 추정하지 않는다. 가능한 대체 검증을 수행하고 상태를 `진행 중` 또는 `막힘`으로 기록한다. 테스트 실패 상태로 다음 작업 단위에 진입하지 않는다.

## 4. 개발일지 자동 작성 규칙

각 작업 단위의 구현과 검증이 끝나는 즉시, 사용자에게 별도로 요청하지 않고 `docs/dev-logs/`에 Markdown 개발일지를 생성한다. 작업이 완료되지 않은 채 세션을 끝내야 할 때도 같은 형식으로 중간 일지를 남긴다.

파일명은 다음 규칙을 따른다.

```text
docs/dev-logs/YYYY-MM-DD_HHmm_<작업ID>_<상태>.md
```

- 시간대: `Asia/Seoul`
- 작업 ID: `docs/DEVELOPMENT_PRIORITY.md`의 ID 사용
- 상태: `complete`, `in-progress`, `blocked` 중 하나
- 같은 작업을 다시 수행하면 기존 일지를 덮어쓰지 않고 새 파일을 만든다.

개발일지에는 반드시 다음 내용을 구체적으로 작성한다.

1. **작업 개요**: 무엇을 만들었는지와 작업 범위
2. **변경 사항**: 주요 파일과 동작 변화
3. **구현 판단**: 선택한 구조와 그 이유
4. **문제 및 막힌 지점**: 무엇이 문제였고 어디서 막혔는지
5. **해결 방법**: 원인, 시도, 최종 해결책
6. **검증 결과**: 실행한 명령·수동 시나리오와 실제 결과
7. **미해결 사항 및 위험**: 남은 문제, 임시 처리, 외부 의존성
8. **다음 작업 지침**: 다음 작업 ID, 시작 파일, 주의점, 재사용할 결정

`문제 없음`으로 끝내기 전에 경계 조건, 도구 제약, 추후 위험을 검토한다. 실제 문제가 없었다면 그렇게 명시하고 확인한 위험을 별도로 적는다. 형식은 `docs/DEVELOPMENT_LOG_TEMPLATE.md`를 사용한다.

## 5. 세션 종료 및 다음 세션 인계

- 작업 단위가 구현되고 테스트까지 통과하면 개발일지를 저장한 뒤 현재 세션을 종료한다.
- 같은 세션에서 다음 작업 단위를 시작하지 않는다.
- 최종 응답에는 완료한 작업 ID, 핵심 검증 결과, 생성한 개발일지 경로, 다음 작업 ID를 알린다.
- 작업이 미완료이면 상태를 `진행 중` 또는 `막힘`으로 기록하고, 재현 방법과 바로 이어서 할 첫 행동을 남긴다.
- 다음 세션은 최신 개발일지를 참고하되, `docs/DEVELOPMENT_PRIORITY.md`의 선행 조건과 실제 코드 상태를 다시 확인한 후 이어서 진행한다.
- 최종 인계에는 아래 형식을 그대로 포함한다.

```text
최종 배포(URL): <Vercel 프로덕션 URL> — Vercel 플러그인 확인 결과
데이터베이스: <Supabase 프로젝트> — 회원: 사용 안 함 / 데이터: notices / 운영로그: crawl_runs / 권한 검증 결과
공지사항 크롤링: <Apify Actor/run ID 3개> — 수동 실행 시각·출처별 결과·Supabase 반영 확인 결과
```

## 6. 우선순위 변경 규칙

외부 키·계정·콘텐츠가 없어 현재 작업이 진행되지 않으면 다음과 같이 처리한다.

1. 가짜 성공 처리를 만들지 않는다.
2. 임시 자산이나 명시적인 mock으로 독립 개발이 가능하면 그 범위만 수행한다.
3. 막힌 이유와 필요한 입력을 `blocked` 일지에 기록한다.
4. 선행 조건이 충족된 다른 작업으로 순서를 바꾸려면 그 이유와 영향을 일지에 남긴다.
5. P0 핵심 흐름을 약화시키는 우선순위 변경은 하지 않는다.
