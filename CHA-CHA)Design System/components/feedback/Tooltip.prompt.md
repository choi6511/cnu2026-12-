Wrap any trigger. Keep the label to a few words — it is a name, not a description.

```jsx
<Tooltip content="PNG 다운로드"><IconButton label="다운로드"><i data-lucide="download" /></IconButton></Tooltip>
```
