Gallery tile: artwork on a white card with its canonical label underneath.

```jsx
<MascotCard name="cheering" height={180} />
<MascotCard name="joy" height={110} tinted onClick={pick} selected />
```

Labels default to the brand sheet's own names (기본 / 기쁨 / 응원하는 차차 / FRONT …) — don't rename them.
