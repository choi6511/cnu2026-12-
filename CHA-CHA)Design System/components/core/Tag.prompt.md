Chip for filters and keyword lists. Pass `onClick` to make it a toggle, `selected` for the on state (navy fill), `onRemove` for a dismissible chip.

```jsx
<Tag selected onClick={() => {}}>표정</Tag>
<Tag variant="dashed">포즈</Tag>
<Tag onRemove={() => {}}>PNG</Tag>
```
