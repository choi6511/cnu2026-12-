Status marker. `solid` for emphasis, `soft` for inline metadata.

```jsx
<Badge tone="teal">NEW</Badge>
<Badge tone="navy" variant="soft" size="md">PNG · 2400px</Badge>
```

Keep blush for gentle emphasis only — it is the cheek color, not a status color.
