# 충남대학교 캠퍼스 탐방 PWA — 제품 요구사항 정의서

## 1. 문서 정보

- 문서 상태: 인터뷰 완료 후 구현용 최종안
- 프로젝트 유형: 24시간 내 제작하는 3인 팀 부트캠프 프로토타입
- 대상 팀: 웹 개발 경험이 많지 않은 학부생 3인
- 제품 형태: 설치 가능한 모바일 우선 PWA
- 배포 방식: GitHub 저장소와 Vercel 연동
- 주요 기술: Next.js, TypeScript, 카카오맵 JavaScript API, Supabase

## 2. 한 줄 정의

충남대학교 신입생이 실제 캠퍼스 장소를 지도에서 발견하고, 장소에서 찍은 사진으로 고유 캐릭터를 수집하며, 장소별 최근 7일 공지를 한곳에서 확인하는 캠퍼스 탐방 PWA이다.

## 3. 해결하려는 문제

충남대학교 캠퍼스는 넓고 시설이 많아 신입생이 주요 장소의 위치와 역할을 익히기 어렵다. 또한 도서관, 국제언어교육센터, 산학협력단 등 기관별 공지 홈페이지가 분리되어 있어 새로운 공지를 일일이 찾아보기 번거롭다.

이 서비스는 다음 두 문제를 함께 해결한다.

1. 실제 지도와 장소 소개를 통해 주요 시설의 위치와 역할을 알려준다.
2. 방문 인증과 캐릭터 수집을 통해 학생이 직접 캠퍼스를 탐방하도록 동기를 부여한다.
3. 서로 다른 기관의 최근 공지를 장소별로 모아 쉽게 확인하게 한다.

## 4. 목표 사용자

### 주 사용자

- 충남대학교에 처음 입학해 캠퍼스가 익숙하지 않은 신입생

### 사용 상황

- 캠퍼스의 주요 시설 위치와 역할을 알고 싶을 때
- 지도에서 목적지를 찾고 장소 정보를 확인할 때
- 장소를 실제로 방문한 뒤 탐방 기록을 남기고 싶을 때
- 기관별 최근 공지를 빠르게 확인하고 싶을 때

## 5. 핵심 시연 장면

발표에서 반드시 성공해야 하는 단일 사용자 흐름은 다음과 같다.

> 사용자가 카카오맵에서 장소를 선택하고 소개를 확인한 뒤, 방문 인증 화면에서 카메라로 사진을 촬영하거나 사진첩의 사진을 선택한다. `인증 완료`를 누르면 해당 장소의 고유 캐릭터가 등장하고 캐릭터 도감에 저장된다.

이 흐름은 설명을 제외하고 2분 이내에 완료되어야 한다.

## 6. MVP 범위

### 6.1 대상 장소

프로토타입에는 다음 세 장소만 포함한다.

| 장소 ID | 표시 이름 | 공지 출처 | 캐릭터 콘셉트 |
| --- | --- | --- | --- |
| `library` | 충남대학교 중앙도서관 | <https://library.cnu.ac.kr/bbs/list/1> | 안경, 책, 사서 또는 독서가 의상 |
| `language-center` | 충남대학교 국제언어교육센터 | <https://dream.cnu.ac.kr/bbs/list.php?wcode=02> | 헤드셋, 말풍선, 여행가방 또는 다국어 학습자 의상 |
| `industry-center` | 충남대학교 산학연교육연구관 | <https://iuc.cnu.ac.kr/iuc/customer/notice.do> | 연구 가운, 노트북, 전구 또는 톱니바퀴 소품 |

자연사박물관은 기존 아이디어에서 완전히 제외한다.

### 6.2 포함 기능

- 카카오맵 기반 실제 지도
- 세 장소의 고정 마커
- 장소별 소개 화면
- 장소별 최근 7일 공지 목록
- 매일 18시 공지 크롤링
- 카메라 촬영 및 사진첩 선택
- 사진 제출 시 장소별 캐릭터 최초 1회 획득
- 캐릭터 도감
- 인증사진과 획득 기록의 기기 내 저장
- 홈 화면 설치가 가능한 PWA
- 장소 소개와 캐릭터 도감의 제한적 오프라인 지원

### 6.3 명시적 제외 기능

- GPS 기반 방문 확인
- QR 코드 방문 확인
- AI 이미지 분석 또는 건물 판별
- 회원가입과 로그인
- 사용자별 서버 저장 및 여러 기기 동기화
- 인증사진의 서버 업로드
- 캐릭터 레벨, 중복 획득, 무작위 뽑기
- 전체 공지 통합 탭
- 푸시 알림
- 공지 본문 복제 또는 앱 내부 전체 본문 표시
- 관리자 페이지
- 세 장소 외 추가 장소

## 7. 정보 구조와 화면

하단 내비게이션에는 `지도`와 `캐릭터 도감` 두 메뉴만 둔다.

### 7.1 지도 화면 `/`

필수 요소:

- 충남대학교 캠퍼스를 중심으로 표시하는 카카오맵
- 중앙도서관, 국제언어교육센터, 산학연교육연구관 마커
- 장소 이름을 확인할 수 있는 마커 라벨 또는 정보창
- 마커 선택 시 해당 장소 소개 화면으로 이동
- 네트워크가 없으면 지도를 대신하는 오프라인 안내

지도 좌표는 개발 전에 카카오맵에서 직접 확인해 정적 장소 데이터에 저장한다. GPS 권한은 요청하지 않는다.

### 7.2 장소 소개 화면 `/places/[slug]`

처음에는 장소 소개만 보여준다.

필수 요소:

- 장소 대표 이미지
- 장소명
- 한두 문단의 짧은 소개
- 주소 또는 캠퍼스 내 위치 설명
- `최근 공지 보기` 버튼
- `방문 인증하기` 버튼
- 캐릭터를 이미 획득한 장소라면 `캐릭터 획득 완료` 상태 표시

장소 소개 문구와 이미지는 앱에 정적으로 포함해 오프라인에서도 표시한다.

### 7.3 최근 공지 화면 `/places/[slug]/notices`

필수 요소:

- 선택한 기관 이름
- 마지막 성공 갱신 시각
- 최근 7일 공지 목록
- 공지별 제목과 작성일
- `원문 보기` 링크
- 공지가 없을 때의 빈 상태 문구
- 오프라인일 때 인터넷 연결 필요 안내

원문 링크는 새 브라우저 탭에서 기관 홈페이지를 연다. 공지 전체 본문은 앱에 저장하거나 복제하지 않는다.

### 7.4 방문 인증 화면 `/places/[slug]/verify`

필수 요소:

- `카메라로 촬영` 버튼
- `사진첩에서 선택` 버튼
- 선택한 사진 미리보기
- `다시 선택` 버튼
- `인증 완료` 버튼
- 지원하지 않는 파일 또는 이미지 처리 실패 안내

동작:

1. 사진이 선택되기 전에는 `인증 완료` 버튼을 비활성화한다.
2. 카메라 버튼은 가능한 기기에서 후면 카메라 촬영을 유도한다.
3. 사진첩 버튼은 기존 이미지 파일을 선택한다.
4. 선택한 파일이 이미지인지 확인한다.
5. 브라우저에서 사진을 최대 1280px 크기로 축소하고 JPEG/WebP 품질 약 0.75로 압축한다.
6. 사용자가 `인증 완료`를 누르면 압축한 사진과 획득 정보를 IndexedDB에 저장한다.
7. 해당 장소의 캐릭터를 처음 획득한 경우에만 획득 연출을 표시한다.
8. 이미 획득한 장소에서는 새 캐릭터를 지급하지 않는다. MVP에서는 기존 인증사진도 교체하지 않는다.

사진 내용이 실제 장소인지 판별하지 않는다. 사진 제출 자체를 방문 인증으로 간주한다.

### 7.5 캐릭터 획득 연출

최초 인증 성공 직후 전체 화면 또는 큰 모달로 다음을 표시한다.

- 장소별 캐릭터 이미지
- `새 캐릭터를 획득했어요!` 문구
- 캐릭터 이름 또는 장소 이름
- `도감에서 확인하기` 버튼
- `지도로 돌아가기` 버튼

복잡한 애니메이션은 필수가 아니다. CSS 기반 확대 및 페이드 인 효과 정도로 제한한다.

### 7.6 캐릭터 도감 `/collection`

세 개의 고정 슬롯을 표시한다.

- 미획득: 어두운 실루엣, 장소 이름, `아직 발견하지 못했어요` 문구
- 획득: 캐릭터 이미지, 장소 이름, 획득 날짜
- 획득한 카드를 선택하면 해당 인증사진을 함께 표시
- `3개 중 N개 획득` 진행 상태

도감은 서버에 연결하지 않고 IndexedDB의 로컬 데이터를 읽는다.

## 8. 캐릭터·이미지 자산

팀이 추후 제공할 학교 마스코트의 360도 캐릭터 시트를 참조해 AI로 장소별 캐릭터 세 개를 제작한다.

요구사항:

- 기본 얼굴, 체형, 색상과 비율은 동일한 캐릭터로 유지
- 장소별 의상과 소품만 다르게 표현
- 카드와 획득 모달에 사용할 수 있는 투명 배경 PNG 또는 WebP
- 세 이미지의 캔버스 크기와 캐릭터 배치 통일
- 학교 마스코트 사용 및 2차 제작에 필요한 권리·허가는 팀이 확인

캐릭터 시트가 준비되기 전에는 동일한 파일명과 비율의 임시 이미지를 사용한다. 최종 이미지는 코드 수정 없이 파일 교체만으로 반영할 수 있어야 한다.

권장 파일명:

- `/public/characters/library.webp`
- `/public/characters/language-center.webp`
- `/public/characters/industry-center.webp`

## 9. 공지 크롤링 요구사항

### 9.1 실행 일정

- 매일 18:00 Asia/Seoul 실행
- Vercel Cron은 UTC 기준이므로 `0 9 * * *` 사용
- 프로덕션 배포의 `/api/cron/notices` GET Route Handler 호출
- `Authorization: Bearer <CRON_SECRET>` 검증 실패 시 HTTP 401 반환
- 개발 중에는 별도의 보호된 수동 실행 또는 로컬 스크립트로 검증

### 9.2 수집 항목

각 공지에서 다음 값만 수집한다.

- 기관/장소 ID
- 제목
- 게시일
- 원문 절대 URL
- 수집 시각

공지 본문과 첨부파일은 수집하지 않는다.

### 9.3 크롤러 구조

세 홈페이지의 HTML 구조가 서로 다르므로 하나의 범용 선택자에 의존하지 않는다. 소스별 어댑터 세 개를 둔다.

- `crawlLibraryNotices()`
- `crawlLanguageCenterNotices()`
- `crawlIndustryCenterNotices()`

각 어댑터는 동일한 `NoticeInput[]` 형식으로 결과를 반환한다. 실제 CSS 선택자는 개발자가 각 페이지 HTML을 확인한 뒤 고정하며, 추측한 선택자를 사용하지 않는다.

크롤링 규칙:

- 서버에서만 외부 홈페이지를 요청해 브라우저 CORS 문제를 피한다.
- 기관 서버에 대한 요청은 하루 한 번으로 제한한다.
- 상대 링크는 절대 URL로 변환한다.
- 게시일을 `Asia/Seoul` 기준 날짜로 정규화한다.
- 세 사이트는 `Promise.allSettled`와 같은 방식으로 독립 실행한다.
- 한 사이트 실패가 다른 사이트의 갱신을 막지 않아야 한다.
- HTTP 오류, HTML 파싱 오류, DB 오류를 구분해 기록한다.

### 9.4 최근 7일 규칙

- 화면에는 현재 한국 시간 기준으로 게시일이 최근 7일 이내인 공지만 표시한다.
- 정확한 기준은 `published_at >= 오늘 00:00 KST - 6일`로 하여 오늘을 포함한 7개의 달력 날짜로 정의한다.
- 오래된 공지는 MVP에서 즉시 물리 삭제하지 않고 조회에서 제외한다.
- 고정 공지도 게시일이 최근 7일 이내일 때만 표시한다.
- 날짜를 해석할 수 없는 항목은 저장하지 않고 해당 실행 로그에 파싱 실패로 기록한다.

### 9.5 실패 처리

- 특정 출처의 크롤링이 실패하면 그 출처의 기존 공지를 삭제하거나 덮어쓰지 않는다.
- 화면에는 해당 출처의 마지막 성공 갱신 시각을 계속 표시한다.
- 성공한 출처만 새 데이터로 upsert한다.
- 로그에는 성공 여부, 수집 개수, 오류 요약, 시작·종료 시각을 남긴다.

## 10. Supabase 데이터 설계

Supabase는 공지와 크롤링 실행 상태만 저장한다. 사용자 정보와 인증사진은 저장하지 않는다.

### 10.1 `notices` 테이블

| 컬럼 | 타입 | 설명 |
| --- | --- | --- |
| `id` | `uuid` | 기본키 |
| `source_id` | `text` | `library`, `language-center`, `industry-center` |
| `title` | `text` | 공지 제목 |
| `published_at` | `timestamptz` | 게시일, KST 날짜를 UTC 시각으로 정규화 |
| `original_url` | `text` | 원문 절대 URL |
| `scraped_at` | `timestamptz` | 마지막 수집 시각 |

제약 및 인덱스:

- `unique(source_id, original_url)`
- `index(source_id, published_at desc)`
- 제목, URL, 날짜는 `not null`

### 10.2 `crawl_runs` 테이블

| 컬럼 | 타입 | 설명 |
| --- | --- | --- |
| `id` | `uuid` | 기본키 |
| `source_id` | `text` | 크롤링 출처 |
| `status` | `text` | `success` 또는 `failed` |
| `item_count` | `integer` | 수집한 공지 수 |
| `error_message` | `text` | 실패 요약, 성공 시 null |
| `started_at` | `timestamptz` | 시작 시각 |
| `finished_at` | `timestamptz` | 종료 시각 |

마지막 성공 갱신 시각은 `source_id`별 `status = 'success'`인 가장 최근 `finished_at`으로 계산한다.

### 10.3 보안 원칙

- 두 테이블 모두 RLS를 활성화한다.
- 공개 클라이언트에는 최근 공지와 성공 갱신 시각을 읽는 SELECT만 허용한다.
- INSERT, UPDATE, DELETE는 공개 클라이언트에 허용하지 않는다.
- 쓰기는 Vercel 서버의 Supabase secret key로만 수행한다.
- Supabase secret key와 `CRON_SECRET`은 절대 `NEXT_PUBLIC_` 환경변수로 만들지 않는다.
- 프런트엔드에는 Supabase URL과 publishable key만 노출할 수 있다.
- 새 프로젝트의 Data API 자동 노출 설정에 따라 필요한 `GRANT SELECT`와 RLS 정책을 명시적으로 확인한다.

## 11. 기기 내 데이터 설계

사진 크기 때문에 `localStorage` 대신 IndexedDB를 사용한다.

`CollectionRecord` 예시:

```ts
type CollectionRecord = {
  placeId: "library" | "language-center" | "industry-center";
  acquiredAt: string;
  photoBlob: Blob;
  photoMimeType: string;
};
```

규칙:

- `placeId`를 고유 키로 사용한다.
- 서버로 전송하지 않는다.
- 브라우저 데이터 삭제 또는 앱 제거 시 기록이 사라질 수 있음을 도감에 짧게 안내한다.
- 저장 공간 부족 시 획득 처리 전에 오류를 보여주고 재시도하게 한다.

## 12. PWA와 오프라인 정책

필수 PWA 요소:

- 유효한 Web App Manifest
- 앱 이름, 짧은 이름, 테마 색상, 배경색
- 192px 및 512px 아이콘
- standalone 표시 모드
- HTTPS 환경에서 등록되는 Service Worker
- 설치 가능 여부를 Lighthouse 또는 실제 Chrome에서 확인

오프라인에서 제공:

- 앱 기본 화면 구조
- 세 장소의 정적 소개
- 캐릭터 도감과 저장된 인증사진

오프라인에서 제공하지 않음:

- 카카오맵 타일과 지도 기능
- 최신 공지 조회
- 공지 원문 링크

오프라인 기능에 접근하면 빈 화면 대신 `인터넷 연결 후 지도를 확인할 수 있어요` 또는 `최신 공지는 인터넷 연결이 필요해요`라는 안내를 표시한다.

## 13. 기술 구조

### 프런트엔드

- Next.js App Router
- TypeScript
- 모바일 우선 반응형 CSS
- 카카오맵 JavaScript API는 지도 화면의 Client Component에서만 초기화
- 이미지 입력, 압축, IndexedDB 접근도 Client Component에서 수행
- 정적 장소 정보는 타입이 지정된 로컬 데이터 파일로 관리

### 서버

- Next.js Route Handler `/api/cron/notices`
- Node.js 런타임 사용
- 소스별 크롤러 모듈
- Supabase 서버 클라이언트
- Vercel Cron 하루 한 번 실행

### 환경변수

| 이름 | 공개 여부 | 용도 |
| --- | --- | --- |
| `NEXT_PUBLIC_KAKAO_MAP_KEY` | 공개 | 카카오맵 JavaScript 키 |
| `NEXT_PUBLIC_SUPABASE_URL` | 공개 | Supabase 프로젝트 URL |
| `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` | 공개 | 공지 읽기 |
| `SUPABASE_SECRET_KEY` | 비공개 | 크롤러의 DB 쓰기 |
| `CRON_SECRET` | 비공개 | 예약 API 호출 검증 |

비밀값은 `.env.local`과 Vercel 환경변수에만 저장하고 GitHub에 커밋하지 않는다. `.env.example`에는 값 없이 변수명만 기록한다.

## 14. 사용자 경험과 디자인 원칙

- 휴대폰 세로 화면을 우선한다.
- 핵심 행동 버튼은 한 손으로 누르기 쉬운 하단 영역에 배치한다.
- 장소 소개 화면에서 `최근 공지 보기`와 `방문 인증하기`를 명확히 구분한다.
- 획득 전 캐릭터는 실루엣으로 보여 탐방 동기를 만든다.
- 획득 성공은 색상, 짧은 모션, 명확한 문구로 즉시 피드백한다.
- 로딩, 빈 결과, 오프라인, 오류 상태를 각각 구분한다.
- 캐릭터와 배경의 시각적 매력보다 핵심 흐름의 안정성을 우선한다.

## 15. 기능별 완료 조건

### 지도

- 온라인 Chrome에서 카카오맵이 충남대 캠퍼스를 중심으로 표시된다.
- 세 장소 마커가 모두 보인다.
- 각 마커를 누르면 올바른 장소 소개 화면으로 이동한다.
- 오프라인에서는 이해 가능한 안내가 표시된다.

### 장소 소개

- 세 장소의 이름, 이미지, 소개, 위치 설명이 서로 맞는다.
- 공지 버튼과 인증 버튼이 각 장소의 올바른 화면으로 이동한다.
- 오프라인에서도 소개가 표시된다.

### 공지

- 세 출처에서 제목, 작성일, 원문 URL을 수집한다.
- 최근 7일 조건에 맞는 공지만 보인다.
- 원문 링크가 올바른 페이지를 연다.
- 마지막 성공 갱신 시각이 표시된다.
- 한 출처 실패 시 다른 출처 결과는 정상 갱신된다.
- 실패한 출처의 마지막 성공 데이터는 유지된다.

### 방문 인증

- 카메라 촬영과 사진첩 선택 경로가 모두 존재한다.
- 선택한 사진을 제출 전에 미리 볼 수 있다.
- 사진 없이 인증 완료할 수 없다.
- 사진 제출 시 해당 장소의 캐릭터가 최초 한 번만 지급된다.
- 페이지 새로고침 후에도 획득 기록과 사진이 남는다.

### 캐릭터 도감

- 세 캐릭터 슬롯이 항상 표시된다.
- 미획득 캐릭터와 획득 캐릭터가 시각적으로 구분된다.
- 획득 날짜와 인증사진을 확인할 수 있다.
- 오프라인과 새로고침 후에도 정상 표시된다.

### PWA

- Vercel 프로덕션 URL이 HTTPS로 열린다.
- 최신 Chrome에서 설치 가능한 앱으로 인식된다.
- 홈 화면에서 실행하면 standalone 형태로 열린다.
- 앱 기본 자산과 장소 소개가 오프라인에서 열린다.

## 16. 필수 테스트 시나리오

1. 첫 실행에서 도감의 세 캐릭터가 모두 잠겨 있다.
2. 지도에서 중앙도서관 마커를 눌러 올바른 소개가 열린다.
3. 중앙도서관 공지에는 최근 7일 항목만 보이고 원문 링크가 열린다.
4. 사진첩에서 이미지를 선택해 중앙도서관 캐릭터를 획득한다.
5. 새로고침 후에도 캐릭터와 인증사진이 남아 있다.
6. 같은 장소에서 다시 인증해도 캐릭터 수가 증가하지 않는다.
7. 나머지 두 장소도 각각 다른 캐릭터를 지급한다.
8. 네트워크를 끊었을 때 장소 소개와 도감이 열린다.
9. 네트워크를 끊었을 때 지도와 공지는 안내 화면을 표시한다.
10. 한 크롤러를 의도적으로 실패시켜도 다른 두 출처는 갱신된다.
11. 잘못된 `CRON_SECRET`으로 예약 API를 호출하면 401이 반환된다.
12. Supabase secret key가 브라우저 번들과 GitHub 저장소에 포함되지 않는다.

## 17. 팀 역할 제안

### A — 통합 개발 및 배포

- Next.js 프로젝트 생성과 GitHub 관리
- 카카오맵 연결
- Supabase 연결과 크롤러 통합
- Vercel 배포 및 환경변수 설정
- 최종 병합과 빌드 오류 해결

### B — 화면 및 PWA 기능

- 장소 소개, 공지 목록, 하단 내비게이션 UI
- 방문 인증 화면과 사진 미리보기
- 캐릭터 도감 UI
- PWA Manifest와 아이콘 연결 보조

### C — 콘텐츠·데이터·검증

- 세 장소 소개 문구와 대표 이미지 준비
- 지도 좌표와 공지 원문 링크 검증
- 각 홈페이지의 게시일·제목·링크 구조 조사
- 실제 휴대폰에서 사용자 흐름 반복 테스트
- 발표용 인증사진과 실패 대비 시연 자료 준비

C의 역할은 코딩 보조가 아니라 데이터 정확성과 시연 성공을 책임지는 독립적인 핵심 역할로 정의한다.

## 18. 24시간 구현 순서

### 0–3시간: 골격과 계정 설정

- Next.js TypeScript 프로젝트 생성
- GitHub 저장소와 Vercel 연결
- Supabase 프로젝트 생성
- 카카오맵 JavaScript 키와 허용 도메인 설정
- 정적 장소 데이터와 라우팅 생성

### 3–8시간: 핵심 화면

- 하단 메뉴
- 카카오맵과 세 마커
- 장소 소개 화면
- 도감의 잠금 상태

### 8–13시간: 방문 인증

- 카메라/사진첩 입력
- 미리보기와 이미지 압축
- IndexedDB 저장
- 캐릭터 획득 모달과 도감 연결

### 13–18시간: 공지 수집

- Supabase 테이블, RLS, 읽기 정책
- 세 사이트별 크롤러
- 최근 7일 필터
- 마지막 갱신 시각과 실패 보존
- 예약 API 보호

### 18–21시간: PWA와 배포

- Manifest, 아이콘, Service Worker
- 오프라인 화면
- Vercel Cron 설정
- 프로덕션 배포

### 21–24시간: 검증과 발표 준비

- 실제 휴대폰 Chrome 테스트
- 세 장소 전체 흐름 테스트
- 크롤러 수동 실행과 결과 확인
- 발표용 사진과 캐릭터 이미지 확인
- 네트워크 또는 크롤링 실패 시 보여줄 마지막 성공 데이터 확인

시간이 부족하면 디자인 장식과 애니메이션을 먼저 줄이고, 지도 → 사진 제출 → 캐릭터 획득 → 도감 저장의 핵심 흐름은 유지한다.

## 19. 우선순위

### P0 — 발표 전 반드시 완료

- 카카오맵과 세 마커
- 장소 소개
- 카메라 또는 사진첩에서 사진 선택
- 장소별 캐릭터 최초 1회 획득
- IndexedDB 저장
- 캐릭터 도감
- Vercel 배포

### P1 — 제품 약속을 위해 필요

- 두 가지 사진 입력 방식 모두 지원
- 세 홈페이지 실제 크롤링
- 최근 7일 필터
- Supabase 저장
- 매일 18시 자동 실행
- 마지막 성공 결과와 갱신 시각
- PWA 설치와 제한적 오프라인 지원

### P2 — 시간이 남을 때

- 획득 모달의 추가 애니메이션
- 지도 마커 커스텀 이미지
- 세밀한 반응형 레이아웃
- 접근성 문구와 키보드 탐색 개선

## 20. 위험과 대응

| 위험 | 대응 |
| --- | --- |
| 기관 홈페이지 HTML 구조가 서로 다름 | 출처별 크롤러 어댑터 분리 |
| 발표 당일 기관 사이트 장애 | Supabase의 마지막 성공 결과 유지 |
| 크롤러 한 개가 전체 실행을 중단 | 출처별 독립 실행과 개별 오류 기록 |
| 휴대폰 사진 용량이 큼 | 클라이언트 압축 후 IndexedDB 저장 |
| 브라우저 저장 데이터 삭제 | 로컬 전용이며 삭제될 수 있음을 안내 |
| 카카오맵 도메인 설정 오류 | localhost, Vercel preview, production 도메인을 사전에 등록 |
| PWA가 기기별로 다르게 동작 | 발표용 실제 기기의 최신 Chrome을 최우선 테스트 |
| 캐릭터 최종 이미지 준비 지연 | 동일 규격 임시 이미지로 기능 개발 후 파일만 교체 |
| 24시간 내 모든 기능 미완성 | P0 흐름을 먼저 완성하고 P1을 순차 추가 |

## 21. 최종 성공 기준

다음 조건을 모두 만족하면 프로토타입을 성공으로 본다.

1. 사용자가 실제 지도에서 세 장소를 발견할 수 있다.
2. 장소를 선택해 소개와 최근 공지로 이동할 수 있다.
3. 사진 촬영 또는 사진 선택 후 장소별 고유 캐릭터를 획득할 수 있다.
4. 획득 기록과 인증사진이 같은 기기의 도감에 유지된다.
5. 세 기관의 최근 7일 공지를 매일 18시에 자동 수집한다.
6. 공지 수집 실패 시 마지막 성공 결과와 갱신 시각을 보여준다.
7. 서비스가 Vercel에 배포되고 Chrome에서 PWA로 설치된다.

## 22. 인터뷰 종료 후 적용한 기본값

인터뷰가 조기 종료되어 답변하지 않은 세부사항에는 다음 기본값을 적용한다.

- 주 테스트 환경: 최신 Android Chrome과 데스크톱 Chrome, 다른 브라우저는 최선 지원
- UI 언어: 한국어
- 시간대: Asia/Seoul
- 사용자 위치 권한: 사용하지 않음
- 재인증: MVP에서는 제공하지 않음
- 공지 정렬: 게시일 최신순
- 공지 중복 기준: 출처 ID와 원문 URL
- 서비스 이름과 색상: 구현 시작 시 임시 이름과 중립적인 충남대 계열 색상을 사용하고 팀이 나중에 교체
- 접근성: 버튼 텍스트, 이미지 대체문구, 색상 외 상태 표시를 기본 적용

